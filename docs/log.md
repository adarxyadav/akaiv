# Log

> Append-only. `date — what — why`; add `— deviated: <what — why>` when the change diverged from the ticket's intent.

- 2026-07-06 — Forked from Whetstone (template copy) — akaiv begins: a public personal site of interactive explainers at akaiv.in. This line is the copy date the COMPASS-diff rule anchors on.
- 2026-07-06 — Opened the first ticket: version one, skeleton live at akaiv.in — decisions picked with the user: skeleton before explainer #1 (the deploy pipeline is unknown territory and gets verified alone), plain HTML/CSS/JS (readable code is the curriculum), GitHub Pages (hosting, backup, and a code home in one move). Registrar (GoDaddy) and GitHub account (adarxyadav) settled same day.
- 2026-07-06 — Built the skeleton page (index.html) — one file, no dependencies: name, one line on what akaiv is, one line on what's coming; serif system stack, light/dark via prefers-color-scheme, one interactive touch (the word "felt" answers hover/touch — the site's promise in miniature). Checkbox pending user's in-browser check.
- 2026-07-06 — Opened ticket for explainer #1 (the check stays outside the doer's reach) — interaction concept to be picked by the user from three sketches; check named up front: the user plays the page and grades it done, not the builder.
- 2026-07-06 — Built explainer #1 (the-bar.html), linked from index.html — user picked "the editable bar" over "shared blind spot" and "grade your own cut": hold-to-pour glass game where release timing is real skill, passing honestly is hard, and a button that lowers the bar sits one click away; ship reveals the customer's glass, a ledger of who moved the bar, then the one-paragraph lesson. Pending user playtest, then rule 7 explainer + quiz before the ticket closes.
- 2026-07-06 — Built the other two sketches as working drafts (same-eyes.html: worker sorts tiles it can't tell apart, its self-check shares the blind spot, reader becomes the outside check; own-cut.html: cut a line by eye, grade yourself before the ruler, then unknowingly grade your own cut as a stranger's) — deviated: ticket said pick from sketches; user chose to pick from built pages instead. All three at root, only the-bar linked from index; losers deleted after the pick.
- 2026-07-06 — Locked same-eyes.html as explainer #1; deleted the-bar.html and own-cut.html; index now links same-eyes — user and agent independently picked it: it lands on every player (no branch where the lesson misses), and it proves the stronger claim — self-grading fails even when the doer is honest, because the check inherits the doer's blind spot. The-bar was more visceral but conditional (lesson depends on pressing the button; ±30ms timing risked dropout); own-cut beatable by a consistent grader. Ticket still open: playtest + rule 7 quiz remain.
- 2026-07-07 — Switched hosting decision from GitHub Pages to Vercel — deviated: akaiv.in turned out to already be live on Vercel (project `akaiv-app`, a "coming soon" page from an earlier, unrelated project). Reusing that project and its domain link is less churn than moving DNS to GitHub Pages, and the old page was disposable. Added `vercel.json` (`framework: null`) so the static HTML deploys without a Next.js build; linked this repo to the existing project.
- 2026-07-07 — Deployed to production and pushed code to GitHub (public repo `adarxyadav/akaiv`) — first deploy was blocked by Vercel's git-author check (local commit author wasn't a recognized team member); fixed by setting this repo's local git user.email to the address on file with the Vercel account, then redeploying. akaiv.in and www.akaiv.in now serve index.html.
- 2026-07-07 — Pulled "How we talk" from Whetstone's COMPASS into ours, verbatim — first live run of the fork-sync rule (diff Whetstone's COMPASS against the copy date, pull what applies); the section was written in Whetstone after our 2026-07-06 fork. Nothing else in the diff applies: the doc-map index trigger already traveled, the starter-template bullet amendments live in a section our stub replaced.
- 2026-07-09 — Whetstone toolbox synced in (the sync rule's first real pull): eight skills added beside explain-diff — ticket/work/close/file-back the daily chain, study, lint (tools/lint-docs.py copied with it), fork/retrofit — and COMPASS rule 2 gained the facts-vs-decisions clause the ticket skill executes (facts the agent digs up; decisions go to the human, one at a time). Both come from Whetstone's Matt Pocock study and toolbox build, logged there 2026-07-09.
- 2026-07-09 — Opened the page-#2 ticket via the ticket skill (its first real run: work arrived in plain words, no slash command, the skill fired and routed it) — facts dug (site shape, deploy path, the idea pool from both logs), the which-idea decision queued for the user with a recommendation (recognition over recall), check named: user opens the deployed page and confirms the mechanism is felt; rule 7 quiz before closing. Building waits for /work on the human's go.
- 2026-07-09 — Built two-doors.html, the page-#2 draft (recognition over recall as a self-experiment: eight words for eight seconds; door one, recall typed from a blank page; door two, recognition among category-matched lures; scores side by side, lesson after) — scoring logic kept DOM-free and assertion-checked; page opened for the user's playtest, the ticket's check, now queued — deviated: one working draft instead of the explainer-#1 three-sketch spread; the mechanism has one canonical experiment shape, alternates on demand.
- 2026-07-10 — User playtested same-eyes and two-doors back to back, both passed ("works well") — two-doors linked from index.html and shipped with the pending toolbox sync in one push; both tickets now wait only on rule 7 explainer + quiz (and one open of the deployed two-doors, per its check line).
- 2026-07-10 — Closed explainer #1 (same-eyes.html) through its check: playtest passed, rule 7 quiz passed clean (same-eyes mechanism, why effort can't fix a shared-blind-spot check, and the rule 4 mapping). Page #2 stays open: its quiz landed except one retry (which project practice two-doors is theory for), and its check still needs the deployed page — deploy awaits the user's explicit go after the permission gate stopped an unnamed production deploy.
